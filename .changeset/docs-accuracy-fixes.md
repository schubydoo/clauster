---
default: patch
---

Correct accuracy drift in the public docs (changeset-bot flow, Tier-A editable allowlist, restart/KillMode survival, doctor warnings, bridge-log glob, example config) and add a drift guard for the editable-fields table.
