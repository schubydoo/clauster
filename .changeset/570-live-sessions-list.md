---
default: minor
---

List every live session under a standard `claude remote-control` bridge: an Active-session card now expands to enumerate each working session with a short-UUID label, uptime, and a per-session deep link into the Claude web app.
