---
default: security
---

A stray OSC 8 (terminal hyperlink) opener can no longer swallow the real authorize URL, because its parameter run now excludes ESC. The live pty screen also re-redacts each row after the width re-fit, so truncation can no longer shear a redacted identifier back into view.
