---
default: patch
---

A stray OSC 8 hyperlink opener can no longer swallow the real authorize URL — its parameter run now excludes ESC — and the live pty screen re-redacts each row after the width re-fit, so truncation can no longer shear a redacted identifier back into view.
