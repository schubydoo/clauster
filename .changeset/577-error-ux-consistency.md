---
default: patch
---

Two dashboard error-UX fixes: the default-session Stop button now asks for confirmation (matching every other destructive action), and error toasts now persist until dismissed instead of auto-vanishing after 4.5 s.
