---
default: patch
---

The live terminal-screen view now masks a real identifier a consumed escape welded onto the word before it. It also masks an id welded to another id, and it matches the real id shape, so an ordinary name like `resolve_session_transcript` stays readable.
