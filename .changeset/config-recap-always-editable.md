---
default: patch
---

The "Recap prior transcript on restart" config toggle is now always editable instead of greyed out when the default launch mode isn't `standard`; an informational note explains it applies to standard bridges only.
