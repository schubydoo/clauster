---
default: minor
---

Show the `claude` version each running bridge is actually on as a small muted label on its live session card, read from the live process tree and left blank when it can't be resolved.
