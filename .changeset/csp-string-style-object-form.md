---
default: patch
---

Apply dynamic dashboard styles via object-form Alpine `:style` so the strict nonce-gated `style-src` (no `unsafe-inline`) no longer blocks the progress-bar fill and the non-name sort layout.
