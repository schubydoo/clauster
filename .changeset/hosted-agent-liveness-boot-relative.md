---
default: patch
---

A Direct Session started after this release keeps its Kill and Resume buttons when the host clock is corrected, instead of being reported as lost while it keeps running. Sessions started before it are unchanged until their next spawn.
