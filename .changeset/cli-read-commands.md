---
default: minor
---

New headless read commands — `clauster projects`, `status`, `sessions`, `logs`, and `open` — drive clauster from the terminal with no server running (add `--json` for scriptable output), sharing one engine with the web UI.
