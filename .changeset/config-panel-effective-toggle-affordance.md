---
default: patch
---

The config panel's "Show merged (effective) view" toggle now carries a disclosure caret and `aria-expanded` — matching the panel's other expand/collapse toggles — instead of rendering as easy-to-miss plain text.
