---
default: patch
---

The config panel's "Show merged (effective) view" toggle now reads as a button — it gains outline chrome, a disclosure caret that reflects its state, and `aria-expanded` — instead of rendering as easy-to-miss plain text.
