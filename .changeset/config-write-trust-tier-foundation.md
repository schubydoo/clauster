---
default: minor
---

Add the fail-closed `config_write` trust-tier foundation (off-by-default capability + scope gate, type-the-name confirm, structural secret redaction, and a shared `~/.claude.json` lock+merge+atomic writer factored out of trust.py)
