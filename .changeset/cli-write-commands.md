---
default: minor
---

New headless write commands — `clauster start <project>` and `clauster stop <instance>` — spawn and stop bridges from the terminal with no server running, sharing the web UI's engine, mode policy, and validation (`--mode`, `--trust`, `--json`).
