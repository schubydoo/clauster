---
default: patch
---

Align the README "First bridge in 60 seconds" walkthrough with the shipped UI: "Run Claude here" replaces the removed "Start" button, "Open in Claude" fixes the link label, and the dead "Start new session" step is replaced with the actual forget-and-relaunch path.
