---
default: minor
---

Add a **No forced mode** (`inherit`) launch choice that passes no `--permission-mode` flag at all, so a session starts in its own default mode instead of one forced into its spawn-time system prompt.
