---
default: minor
---

Add an opt-in, auth-gated dashboard panel that drives a `claude auth login` / `claude setup-token` flow so an operator can re-authenticate the runtime `claude` account from the browser instead of SSHing in.
