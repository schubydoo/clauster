---
default: patch
---

The dashboard now wraps its content in a single `<main>` landmark and carries a page `<h1>`, so assistive tech can navigate its regions and every content node sits inside a landmark (resolves the dashboard's remaining moderate axe landmark/heading findings).
