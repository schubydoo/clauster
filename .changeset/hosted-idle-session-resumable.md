---
default: patch
---

A Direct Session (hosted) that is stopped before its first turn is now resumable, instead of ending as an unrecoverable crash — clauster pins the session id at spawn so the conversation can always be reloaded.
