---
default: patch
---

Correct the Claude Code install steps (native installer; npm is deprecated) and the clone dialog's helper text (name the `clone.allow_private_hosts` clauster.yml key; clarify cloning only fetches files).
