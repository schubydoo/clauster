---
default: minor
---

Add an in-app help page: a `?` icon in the top-right of every page (dashboard, login, 404) opens a keyboard-accessible Tabler offcanvas (Esc / click-away to close, focus-trap, focus restored to the trigger) with hand-authored guidance on what Clauster is, the launch modes (standard / pty / browser), permission modes, the dashboard zones, the managed-vs-External session distinction, the header tools, and a link out to the README.
