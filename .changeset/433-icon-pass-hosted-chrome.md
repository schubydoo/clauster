---
default: patch
---

Dashboard (hosted live view): the tool and permission-decision badges in the hosted transcript now use Tabler sprite icons instead of Unicode emoji (🔧 → `tool`, ✓/✕ → `check`/`x`), matching the sprite-icon system the rest of the UI uses.
