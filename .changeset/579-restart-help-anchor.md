---
default: patch
---

Fix the config-editor "restart Clauster to apply" help link, which pointed at a nonexistent README anchor; it now targets the Operations runbook's restart section, which carries a stable custom anchor to prevent future rot.
