---
default: patch
---

Give every dashboard form control a stable `id`/`name` and an accessible name, clearing the browser's form-field accessibility advisories for screen-reader and password-manager users.
