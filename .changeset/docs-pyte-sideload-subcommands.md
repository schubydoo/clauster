---
default: patch
---

Docs: the `pty_screen_enabled` reference now documents the `CLAUSTER_PYTE_PATH` side-load on the standalone binary (#702) instead of claiming the live view is impossible there, and the install guide lists `clauster hash-metrics-token` + `clauster config reconcile`.
