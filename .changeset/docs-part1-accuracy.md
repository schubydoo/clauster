---
default: patch
---

Docs accuracy: warn that the on-disk bridge log is not redacted by default (SUPPORT + operations), and replace the stale README claim that `claustrum` is not distributable with the `clauster deps install claustrum` instruction.
