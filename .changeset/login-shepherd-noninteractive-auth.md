---
default: minor
---

The dashboard login panel now points operators at the non-interactive runtime-account auth options (`ANTHROPIC_API_KEY`, `CLAUDE_CODE_OAUTH_TOKEN`, `apiKeyHelper`) so they can be set from the config editor without SSH, with docs on which to use when.
