---
default: patch
---

A zero-width or invisible Unicode character inside a session or environment identifier no longer hides it from the log redaction. Such characters are now removed from the redacted log the dashboard shows.
