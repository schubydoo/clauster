---
default: patch
---

Unify the hosted-session status badge colours with the bridge map in the shared Active list: `starting` is azure, `stopping` is orange, and `crashed` is amber (recoverable, Resume) instead of red — and the crashed badge now matches its dot.
