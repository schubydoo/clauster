---
default: minor
---

Add a `clauster mcp` read-only stdio MCP server exposing `list_sessions` and `session_status` tools that report Clauster's bridge, hosted, background-agent, and external sessions to any MCP client
