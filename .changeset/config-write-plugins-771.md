---
default: minor
---

Add a plugins + marketplaces config-write API (enable/disable/install/uninstall/update, marketplace add/remove/list/update) driven through `claude plugin`, gated behind the existing off-by-default capability plus a strong per-plugin confirm on install.
