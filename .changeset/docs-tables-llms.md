---
default: patch
---

Docs: platform support is now a scannable capability × OS matrix on its own reference page, and the site ships an `llms.txt` map that states the bind/auth safety invariant for assistants.
