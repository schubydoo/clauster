---
default: patch
---

Show the live-session list from the first live session on a standard bridge (not only at two or more), giving each session its own `claude.ai/code` deep link; the toggle label is now singular/plural-aware.
