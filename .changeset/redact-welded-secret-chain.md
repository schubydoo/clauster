---
default: security
---

The streamed log, the on-disk mirror and the live pty-screen view now mask every secret in a chain of secrets written with no separator, and a UUID written directly after a word.
