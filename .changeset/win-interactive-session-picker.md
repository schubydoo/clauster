---
default: patch
---

The dashboard now offers Interactive Session on Windows when the ConPTY keeper (the `pty` extra) is installed, instead of always hiding it — the mode-picker gate was stale after Windows ConPTY support landed.
