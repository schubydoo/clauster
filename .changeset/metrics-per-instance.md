---
default: patch
---

Show each session row its own bridge's CPU/memory instead of the project total, so a Server Mode row no longer reports the co-located Interactive Sessions' usage as its own (Interactive Session rows now show metrics too).
