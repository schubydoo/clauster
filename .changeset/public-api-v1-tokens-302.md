---
default: minor
---

Add a versioned `/api/v1` public API with named, revocable bearer tokens (`clauster api-token issue/list/rotate/revoke`) and an optional OpenAPI docs page (`api.openapi_enabled`, off by default).
