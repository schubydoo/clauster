---
default: minor
---

Add a versioned `/api/v1` public API surface, an off-by-default Bearer-gated OpenAPI docs toggle (`api.openapi_enabled`), and `clauster api-token issue/list/rotate/revoke` for named, revocable bearer tokens.
