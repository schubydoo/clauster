---
default: patch
---

Modals no longer close when a text-selection drag that started inside the dialog releases over the backdrop — only a click that both starts and ends on the backdrop dismisses (Escape and the close button are unchanged).
