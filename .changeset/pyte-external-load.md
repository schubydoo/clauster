---
default: minor
---
Add an opt-in `CLAUSTER_PYTE_PATH` env var so a standalone-binary user can enable the read-only live terminal view by separately installing `pyte` and pointing it at that directory, without bundling LGPL code
