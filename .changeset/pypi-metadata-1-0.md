---
default: patch
---

The PyPI page now renders its README links correctly (they were repo-relative, so every one 404'd there) and the package is classified `Development Status :: 5 - Production/Stable`.
