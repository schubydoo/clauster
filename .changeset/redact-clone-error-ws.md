---
default: security
---

Redact a failed clone's `error_detail` (git stderr tail) on the progress WebSocket, closing a redaction asymmetry with the clone-done webhook path where the same value was already redacted.
