---
default: minor
---

Add a config-write API for `env`/`model`/misc `settings.json` keys at user/project/local scope, plus a scope-merge provenance view showing each key's effective value and source layer.
